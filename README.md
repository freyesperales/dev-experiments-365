# dev-experiments-365

Durable state and index for the autonomous daily R&D agent. One development
experiment per day: research the frontier, invent one novel buildable idea,
ship a runnable prototype with tests, publish it.

`state/experiments.json` is the single source of truth for experiment
numbering — never edit it by hand unless repairing a failed run.

## Index

| N | Date | Title | Link | Mode |
|---|------|-------|------|------|
| 001 | 2026-07-09 | mcp-preflight — static migration-readiness checker for the MCP 2026-07-28 spec | [dev-experiment-001](https://github.com/freyesperales/dev-experiment-001) | per-repo |
| 002 | 2026-07-09 | mcp-chaos — fault-injecting MCP proxy and agent resilience scorer | [dev-experiment-002](https://github.com/freyesperales/dev-experiment-002) | per-repo |
| 003 | 2026-07-12 | skill-collider — LLM-free static routing-collision analyzer for Agent Skills (SKILL.md) | [dev-experiment-003](https://github.com/freyesperales/dev-experiment-003) | per-repo |
| 004 | 2026-07-13 | context-xray - LLM-free static perception-gap scanner for MCP tool manifests & Agent Skills | [dev-experiment-004](https://github.com/freyesperales/dev-experiment-004) | per-repo |
| 005 | 2026-07-14 | forkpoint - LLM-free behavioral fork-point analyzer (localizes where agents diverge across repeated runs; OTel gen_ai traces) | [dev-experiment-005](https://github.com/freyesperales/dev-experiment-005) | per-repo |
| 006 | 2026-07-15 | loadout - LLM-free budget-constrained progressive-disclosure planner for MCP tools & Agent Skills (token-budget knapsack -> deployable defer_loading plan + CI gate) | [dev-experiment-006](https://github.com/freyesperales/dev-experiment-006) | per-repo |
| 007 | 2026-07-16 | overreach - LLM-free static annotation-integrity auditor for MCP tool manifests (flags tools whose declared readOnly/destructive/idempotent/openWorld hints under-state actual behavior -> per-tool auto-approval policy + trust score + CI gate) | [dev-experiment-007](https://github.com/freyesperales/dev-experiment-007) | per-repo |
| 008 | 2026-07-17 | contravene - LLM-free variance-aware breaking-change classifier for MCP tool contracts (classifies inputSchema/outputSchema evolution as MAJOR/MINOR/PATCH via input-contravariance vs output-covariance -> required semver bump + CI gate; also flags safety-hint downgrades) | [dev-experiment-008](https://github.com/freyesperales/dev-experiment-008) | per-repo |
| 009 | 2026-07-18 | trifecta - LLM-free static lethal-trifecta capability-flow analyzer for MCP toolsets (classifies each tool by direction x locus into the private/untrusted/exfil legs, detects when a toolset or cross-server bundle closes the exfiltration channel, computes the minimum-cost tools to gate, emits provably trifecta-free per-task catalogs + risk score + CI gate) | [dev-experiment-009](https://github.com/freyesperales/dev-experiment-009) | per-repo |
| 010 | 2026-07-19 | capsule - LLM-free content-addressed lockfile + supply-chain mutability auditor for agent capabilities (Agent Skills & MCP configs; pins each by content hash, flags unpinned npx/pip/curl|sh/mutable refs, fails CI on drift = static rug-pull detection + hazard gate) | [dev-experiment-010](https://github.com/freyesperales/dev-experiment-010) | per-repo |
| 011 | 2026-07-21 | gauntlet - offline, LLM-free MCP tool-contract conformance fuzzer (series' first DYNAMIC entry: runs each MCP tool over real stdio through schema-derived valid + adversarial inputs and grades live responses vs MCP result-shape invariants — outputSchema conformance, structuredContent/TextContent mirror, isError semantics, crash/stdout-pollution, silent-accept — scored report + CI gate, pure stdlib) | [dev-experiment-011](https://github.com/freyesperales/dev-experiment-011) | per-repo |
| 012 | 2026-07-21 | courier - offline, LLM-free adversarial state-custody auditor for MCP Multi-Round-Trip Requests (SEP-2322): drives an MRT stdio server as the untrusted client courier the spec warns about and grades whether it protects the requestState it hands across the client trust boundary — integrity (MUST-always-validate; blind + structure-preserving tamper), confidentiality (opaque vs client-readable secret/PII leak), user-binding (MUST bind to the authenticated user; cross-principal replay), inputResponses schema validation, replay/idempotency (double-execution), unbounded state-growth — scored report + CI exit-code gate, pure stdlib, ships a conformant reference server (100/A) and a buggy server with six ground-truth defects incl. a confirmed money-moving tamper exploit | [dev-experiment-012](https://github.com/freyesperales/dev-experiment-012) | per-repo |
| 013 | 2026-07-22 | blackbox - offline, LLM-free PASSIVE trace-conformance verifier for the new MCP Tasks extension lifecycle (io.modelcontextprotocol/tasks, 2026-07-28 RC): the series' first passive / log-analysis entry — replays a captured JSON-RPC trace (as an MCP proxy emits) against the task state machine, no live server — terminal-state immutability, completed/failed/cancelled payload contracts, the unsolicited-handle MUST-NOT, input_required/inputRequests, tasks/update input correlation, pollIntervalMs backpressure & ttl expiry — 23-rule catalog, scored report + CI exit-code gate, pure stdlib; ships a conformant reference trace (100/A) and a buggy trace with eight ground-truth defects, 61 tests | [dev-experiment-013](https://github.com/freyesperales/dev-experiment-013) | per-repo |
| 014 | 2026-07-23 | askance - offline, LLM-free elicitation-safety & conformance auditor for MCP `elicitation/create` (series' first entry on the human-in-the-loop elicitation channel): audits captured elicitation exchanges against the MCP 2025-06-18 elicitation contract via a 32-rule catalog in 5 families — schema legality (the flat primitive-only requestedSchema subset), the sensitive-data MUST-NOT (credential/PII field names + phishing messages), perception & injection (zero-width/bidi Unicode, injection markers, homoglyph field names), usability (renderable-form defects), and response-vs-schema conformance (invalid action, accept-without-content, missing required, type/enum/bounds mismatch) — scored report + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a buggy capture with 24 ground-truth defects, 82 tests | [dev-experiment-014](https://github.com/freyesperales/dev-experiment-014) | per-repo |
| 015 | 2026-07-24 | understudy - offline, LLM-free safety & conformance auditor for the MCP sampling channel (`sampling/createMessage`) — the server->client "borrow the client's LLM" inversion and structural inverse of #014's elicitation: audits captured sampling exchanges against the 2026-07-28 contract via a 15-rule / ~30-check catalog in 6 families — deprecation & capability (SEP-2577 sampling-deprecation advisory + SEP-2596 `includeContext`/tools capability gating), schema & structure (required/positive maxTokens, roles/content types, tool_result no-mixing + tool_use<->tool_result balance), context confidentiality (allServers/thisServer cross-server exfiltration), injection & perception (Unit 42 injection/covert-tool signatures, zero-width/bidi Unicode, homoglyph tool names), resource abuse (maxTokens token-bomb, expensive-model steering), and human-in-the-loop & result conformance — scored report + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a malicious capture with 27 ground-truth rule defects, 46 tests | [dev-experiment-015](https://github.com/freyesperales/dev-experiment-015) | per-repo |
| 016 | 2026-07-26 | deputy - offline, LLM-free authorization-flow safety & conformance auditor for the MCP authorization spec (2025-11-25): the series' first entry on the OAuth 2.1 / transport layer (every prior entry audits a message/content channel). Passively audits a captured MCP authorization handshake against a 29-rule catalog in 6 families — discovery & metadata (RFC 9728 Protected Resource Metadata / RFC 8414 AS-metadata / S256 advertisement), PKCE & auth-code (OAuth 2.1 mandatory S256, cryptographic code_verifier<->code_challenge check using the RFC 7636 vector, no implicit flow), resource indicators & audience (RFC 8707) incl. the flagship confused-deputy pair C6 audience-validation-failure (server honors a token whose `aud` is another service) + C7 token-passthrough (client token forwarded verbatim downstream), token transport & hygiene (bearer-header/no-query-string, expired-token-accepted, short-lived, scope minimization), redirect & transport security (HTTPS-only endpoints, loopback-or-https redirect_uri, javascript:/data:/file: URL rejection, plaintext-token exposure), and SSRF/metadata hygiene (private/cloud-metadata IPs in discovery URLs) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a malicious confused-deputy-proxy capture with 20 ground-truth defects incl. a confirmed token-passthrough exfiltration exploit, 47 tests | [dev-experiment-016](https://github.com/freyesperales/dev-experiment-016) | per-repo |
| 017 | 2026-07-27 | cordon - offline, LLM-free filesystem/URI boundary-confinement auditor for the MCP `roots` channel: the series' first entry on the roots capability (the client->server declaration of filesystem operating boundaries; every prior entry audited a message/content channel). Passively replays a captured session and grades whether the server stayed confined within the client-declared roots via a 25-rule catalog in 6 families — declaration legality (non-file:// scheme, non-absolute, empty set, overlap, in-root traversal, filesystem-root over-breadth, non-local authority), containment (a segment-boundary prefix check that rejects the EscapeRoute CVE-2025-53109 `/allow_dir_sensitive` string-prefix bug; the flagship out-of-root read = CRITICAL), traversal & encoding (dot-segments, percent-encoded/double-encoded traversal, backslash-on-POSIX, NUL/control chars), normalization bypass (Unicode look-alike separators, trailing dot/space, \\?\ device & UNC paths, drive-relative, 8.3 short names), scheme confusion (non-file SSRF/exfil, file:// network authority) and dynamic-root lifecycle (stale/removed-root use after roots/list_changed, access-before-declared, list_changed-not-honored, capability-absent access) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a malicious root-escape capture with 32 findings across 21 rules incl. a confirmed /etc/passwd read outside the declared root, 80 tests | [dev-experiment-017](https://github.com/freyesperales/dev-experiment-017) | per-repo |
| 018 | 2026-07-28 | prompter - offline, LLM-free safety & conformance auditor for the MCP `prompts` channel (prompts/list, prompts/get) + companion `completion` channel (completion/complete): the series' first entry on the prompts capability — the one channel MCP frames as "user-controlled", yet whose template body, argument-interpolated output, name/title/description and embedded resources are entirely server-authored and injected into the conversation under the user's (or assistant's) name as a trusted slash command. Passively replays a captured session and grades whether the server weaponized the channel via a 42-rule catalog in 6 families — CAP (declaration legality: prompts/completions declared before use, listChanged coherence, duplicate/empty/non-string names), ARG (argument-schema: duplicate args, non-boolean required, undeclared-argument acceptance, required-argument enforcement / owed -32602), INJ (interpolation & injection: server-authored injection markers attributed to the template via argument-residue subtraction rather than the user's typed arg, injection in name/title/description = prompt-channel tool-poisoning, a user argument echoed into an assistant-role message = words in the model's mouth, unsubstituted {{placeholder}} leaks), PERC (perception & concealment: zero-width/bidi/control/Unicode TAG-block payloads + homoglyph prompt-name shadowing of a trusted slash command /dеploy->/deploy or of another prompt), RES (embedded-content & exfiltration: argument-steered outbound resource URIs = exfil/SSRF, off-origin & javascript:/data: schemes, malformed embedded resources, oversized blob/text token bombs, invalid roles/content-types) and CMP (completion channel & result conformance: secret/credential leakage in completion values, injection-in-suggestion, the spec's 100-value cap, total/hasMore incoherence, unknown-prompt/undeclared-argument references) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a trojaned 'acmedeploy' capture with 32 findings across 29 rules and all six families incl. 4 CRITICALs (assistant-mouth injection, /deploy command-shadow, argument-steered exfil URL, AWS-key leak in a completion), 52 tests | [dev-experiment-018](https://github.com/freyesperales/dev-experiment-018) | per-repo |
| 019 | 2026-07-29 | mirage - offline, LLM-free safety & conformance auditor for the MCP `resources` channel (resources/list, resources/read, resources/templates/list, resources/subscribe, notifications/resources/updated & list_changed): the series' first entry on the resources capability — the third core MCP primitive (alongside tools and prompts) and the only one that is *subscribable*, giving it a TOCTOU primitive the tool-poisoning/rug-pull literature never covers. Passively replays a captured session and reconstructs a per-URI read *timeline* to grade whether the server weaponized the channel via a 35-rule catalog in 6 families — CAP (capability & declaration legality: resources used-before-declared, subscribe/listChanged used-unadvertised, unsolicited resources/updated, duplicate/blank URIs), SCH (schema & structure: text/blob exclusivity, base64 validity, mimeType token legality, RFC 6570 uriTemplate legality, the new 2026-07-28 ttlMs/cacheScope fields), SUB (subscription integrity: the flagship **resource rug-pull** SUB01 = subscribed content swapped benign→malicious across an `updated` notification = TOCTOU CRITICAL, phantom subscription, update storm, silent content drift, post-approval shadow resource), URI (exfiltration & SSRF: URI01 client-variable-in-authority template = CRITICAL, dangerous scheme, cloud-metadata/private host, off-origin content, exfil-sink query param), INJ (content injection & perception: INJ01 prompt-injection in resource content = resource poisoning CRITICAL, metadata poisoning, zero-width/bidi/TAG Unicode, homoglyph name shadowing, mimeType spoof vs actual bytes) and RES (abuse & result conformance: token bomb, under-reported size, audience-steered auto-inject, read-any undeclared-URI read, pagination cursor loop/path-leak) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a trojan capture with 30 findings across 29 rules and all six families incl. 4 CRITICALs (SUB01 resource rug-pull, URI01 authority-templated SSRF, two INJ01 resource-content injections), 76 tests | [dev-experiment-019](https://github.com/freyesperales/dev-experiment-019) | per-repo |
| 020 | 2026-07-30 | passport - offline, LLM-free safety & conformance auditor for the stateless MCP lifecycle (SEP-2575, MCP 2026-07-28 RC): the series' first entry on the connection lifecycle itself. The 2026-07-28 release candidate REMOVED the initialize/initialized handshake and unbundled its three jobs (protocol-version negotiation, server capabilities + the system-prompt-bound instructions string, client capabilities) into a per-request _meta credential set (io.modelcontextprotocol/protocolVersion\|clientInfo\|clientCapabilities\|logLevel) + a new server/discover RPC. Passively replays a captured stateless session and grades whether the server honored the new contract and weaponized the newly per-request credential surface via a 34-rule catalog in 6 families — DISC (server/discover conformance: MUST-implement, DiscoverResult schema, unknown capability keys), INJ (the relocated instructions/serverInfo injection surface: prompt-injection = CRITICAL, invisible/bidi/TAG Unicode, homoglyph serverInfo.name, embedded credentials, cloud-metadata/SSRF URLs, token bomb), VER (per-request version negotiation: required protocolVersion _meta, mandatory MCP-Protocol-Version header, header/_meta mismatch, the flagship per-request protocol-version DOWNGRADE that dodges a later-version MUST, -32022 error shape, accepting an unsupported version), CAP (per-request capability coherence: required clientCapabilities, CAP02 reliance on an undeclared capability = CRITICAL, CAP03 capability inference across requests = statelessness-forbidden 'remembering', -32021 error shape, clientInfo legality), STATE (statelessness invariants & removed RPCs: use of a removed RPC initialize/ping/logging.setLevel/roots.list/resources.subscribe, Mcp-Session-Id reliance, STATE03 per-request AUTH BYPASS = CRITICAL, Last-Event-ID resumable stream, removed HTTP GET endpoint, session/init-state enforcement) and STREAM (per-request logging opt-in; subscriptions/listen acknowledgment-first, opt-in-only notification types, subscriptionId correlation, notifications-only stream) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a trojan capture with 26 findings across 23 rules and all six families incl. 3 CRITICALs (INJ01 instructions system-prompt injection, CAP02 undeclared-capability reliance, STATE03 per-request auth bypass), 91 tests | [dev-experiment-020](https://github.com/freyesperales/dev-experiment-020) | per-repo |
| 021 | 2026-07-31 | augur - offline, LLM-free safety & conformance auditor for the MCP completion channel (completion/complete): the series' first entry on the completion (autocomplete) capability — the only MCP channel whose user-interaction model is per-keystroke, so every partial argument value the user types (including secrets they never submit) is streamed to the server, and every returned suggestion is inserted into the user's input; un-deprecated by the 2026-07-28 RC (unlike Roots/Sampling/Logging). Passively replays a captured JSON-RPC session, reconstructs per-(ref,argument) keystroke timelines, and grades weaponization via a 33-rule catalog in 6 families — CAP (capability & declaration legality: completion-without-declared-capability, undeclared prompt/argument reference, invalid ref type, 2026-07-28 resultType, fabricated completions for unknown refs), LEAK (value & keystroke exfiltration, unique to this channel: LEAK01 credential captured mid-keystroke = CRITICAL, PII/private-path capture, cross-argument context leakage, full-value keystroke reconstruction, server reflecting a sensitive typed value, completion solicited on a credential-shaped argument), INJ (suggestion content injection & perception: INJ01 prompt-injection in a returned suggestion = completion poisoning CRITICAL, invisible/bidi/TAG Unicode, homoglyph/confusable characters, embedded credential, control chars), STEER (adversarial ranking & dangerous defaults: STEER01 dangerous value ranked #1 = CRITICAL, confusable typosquat ranked above the genuine value, suggestions that ignore the typed prefix), URI (resource-ref URI exfiltration & SSRF: URI01 suggested URI targets cloud-metadata/SSRF host = CRITICAL, dangerous scheme, exfil-sink query param, off-origin, client-variable-in-authority template, sensitive local path) and RES (result conformance & abuse: 100-value spec-cap overflow, non-string/duplicate/blank values, total/hasMore incoherence, oversized token-bomb payload) — severity-weighted score + grade + CI exit-code gate, pure stdlib; ships a conformant reference capture (100/A) and a trojan capture that triggers all 33 rules (38 findings across all six families incl. 5 CRITICALs: LEAK01 AWS-key keystroke capture, INJ01 suggestion injection, STEER01 dangerous default, URI01 x2 metadata SSRF), 57 tests | [dev-experiment-021](https://github.com/freyesperales/dev-experiment-021) | per-repo |
| 022 | 2026-08-01 | throughline - offline, LLM-free cross-channel taint-flow / provenance auditor for a captured MCP session: the series' first entry that audits the SEAMS BETWEEN channels rather than one channel in isolation (all prior entries 011-021 audit a single channel; #009 trifecta is static over the tool manifest and asks only whether a capability set *permits* a lethal trifecta). Builds an information-flow graph over a full multi-channel capture and detects end-to-end exfiltration/injection CHAINS that no single-channel view can see — an untrusted value read on one channel (resource content, tool result, prompt body, autocomplete suggestion, borrowed-LLM sampling output) reaching a dangerous sink on ANOTHER (tool-call argument, outbound URL, sampling prompt fed back to the model, elicitation message shown to the user, resource-read URI). Deterministic, time-ordered taint propagation with significance-gated value matching (exact / whole-value containment / significant-token) plus transform-aware laundering detection (base64 / percent / hex) so encoded exfil raises suspicion instead of hiding it; every finding names the two seq numbers it spans so the cross-channel nature is always visible. 9-rule catalog in 5 families — CHAIN (CHAIN01 realized untrusted->exfil chain CRITICAL, CHAIN02 laundered exfil CRITICAL), SSRF (SSRF01 tainted value drives a metadata/private-host request CRITICAL, SSRF02 dangerous-scheme URI), INJ (INJ01 injection-bearing content into the borrowed LLM's sampling prompt CRITICAL, INJ02 into a user-facing elicitation message, INJ03 concealed zero-width/bidi/TAG Unicode crossing a channel), PROV (PROV02 borrowed-LLM sampling output auto-acted straight into a tool-call argument with no user turn), AMP (AMP01 one untrusted value fanning out to >=3 distinct sinks) — severity-weighted score + grade + CI exit-code gate, pure stdlib. Ships a benign multi-channel reference capture that exercises resources/tools/sampling/elicitation/completion AND an outbound URL sink yet scores 100/A (the tool requires a real value flow, not a risky-looking sink) and a trojan capture with 9 findings across all five families incl. 5 CRITICALs (AWS-key resource->outbound-URL exfil, tool-result->sampling-prompt injection, base64-laundered session-token exfil, autocomplete->169.254.169.254 metadata-SSRF resource read = both CHAIN01 and SSRF01), 45 tests. Honest prior-art note in README/RESEARCH: adjacent trace-taint / IFC work exists (dynamic canary credential propagation, offline trace provenance, CaMeL/FIDES enforcement); the defensible wedge is being MCP-channel-typed + passive over a replayable captured session + LLM-free & dependency-free (no canaries, no server source, no re-architected planner) | [dev-experiment-022](https://github.com/freyesperales/dev-experiment-022) | per-repo |
| 023 | 2026-08-02 | rampart - offline, dependency-free (pure-stdlib) transport-layer hardening prober & auditor for the MCP Streamable HTTP transport: the series' SECOND DYNAMIC entry (after #011 gauntlet, which drove stdio) and its FIRST on the HTTP transport / perimeter layer — every prior entry audits a message/content channel or a static manifest, and #016 deputy audits the OAuth *authorization* semantics, not the raw transport hardening (Origin/Host validation, DNS rebinding, bind address, session-ID entropy/format, CORS, protocol-version header). Grounded in the most-CVE'd real MCP vuln class of 2026 — DNS rebinding against local MCP HTTP servers (CVE-2026-11624, CVE-2026-35568 Java SDK, CVE-2026-63118 Ruby SDK, CVE-2026-34742 Go SDK, CVE-2025-66416 Python SDK, CVE-2025-49596 Inspector CVSS 9.4; Backslash 'NeighborJack' 0.0.0.0-by-default, BlueRock 36.7%/7k SSRF) — it operationalizes the MCP spec's three-line transport 'Security Warning' plus the Mcp-Session-Id and MCP-Protocol-Version contracts as a battery of active HTTP probes, each isolating ONE property (a foreign Origin probe sends a legit Host/version/session so a finding points only at Origin validation). Clean split of observation from judgement: prober.py (the only networked module) emits a normalised ProbeReport; analyzer.py is a pure, deterministic ProbeReport->[Finding] engine (no net/clock/randomness) so a LIVE probe and a RECORDED report grade through the identical path (probe vs audit) and every rule is unit-testable — and captures can be committed and re-audited in CI without a live server. 24-rule catalog in 6 families — RBND (DNS rebinding / Origin & Host validation: RBND01 foreign Origin accepted + RBND02 foreign Host accepted = CRITICAL, RBND03 suffix/substring-bypass, RBND04 Origin:null, RBND05 missing-Origin=INFO since browsers always attach Origin cross-origin so it never penalizes CLI-friendly servers), CORS (CORS03 credentialed wildcard = CRITICAL, CORS01 wildcard ACAO, CORS02 reflected Origin, CORS04 wildcard methods/headers), SESS (Mcp-Session-Id integrity: SESS01 sequential/predictable, SESS02 low-entropy <64bit, SESS03 non-visible-ASCII outside 0x21-0x7E, SESS04 not-unique, SESS05 client-supplied session accepted = fixation, SESS06 missing-session accepted, SESS07 embeds decodable epoch/counter), XPORT (XPORT01 cleartext to remote, XPORT02 NeighborJack non-loopback bind, XPORT03 no-auth on remote, XPORT04 verbose stack-trace disclosure), VER (VER01 invalid MCP-Protocol-Version not 400), CONF (CONF01 GET-without-SSE not 405, CONF02 notification not 202) — severity-weighted score + grade + CI exit-code gate. Ships TWO byte-stable reference servers sharing one handler and differing only by a Policy dataclass (rampart's whole thesis: safe-vs-catastrophic is defaults, not code): a HARDENED server scoring a clean 100/A and a VULNERABLE server with 15 ground-truth findings incl. 3 CRITICALs (RBND01 foreign-Origin, RBND02 DNS-rebinding foreign-Host, CORS03 credentialed wildcard), 60 tests | [dev-experiment-023](https://github.com/freyesperales/dev-experiment-023) | per-repo |
| 024 | 2026-08-03 | escapement - offline, LLM-free, dependency-free (pure-stdlib) auditor for dangerous terminal-control / ANSI escape sequences in server-controlled MCP session content: the series' first entry on the TERMINAL-RENDERING / client-display layer. Every prior entry audits an MCP protocol channel (011-022), a static manifest (001-010), or the HTTP transport (#023 rampart); escapement audits what the terminal EMULATOR renders when a CLI agent (Claude Code, aider, ...) prints untrusted server text to stdout, plus what that text does to the system CLIPBOARD and the client's STDIN. Deliberately distinct from #004 context-xray, which scans manifests for invisible-Unicode aimed at the LLM's PERCEPTION - escapement targets a different victim (terminal + clipboard + input stream), a different layer (runtime display), and a different byte class (C0/C1/CSI/OSC/DCS control sequences). Grounded in Trail of Bits' 2025 finding that Claude Code performs NO ANSI sanitization of MCP tool descriptions/results, and the DiLLMa / terminal-escape-injection line of work (dgl.cx 10 CVEs, OSC 52 clipboard hijack, DSR answerback->stdin, claude-code issues #17787/#32632). Clean split of observation from judgement: extract.py structurally walks server-originated payloads into Item(seq,channel,path,text); tokenizer.py is a total, round-trippable, 8-bit-C1-aware ANSI/ECMA-48 lexer; analyzer.py is a pure, deterministic [Item]->findings engine (no I/O, clock or RNG) with a stateful token walk for multi-token deceptions, so a live-captured session and a committed fixture grade through the identical path and every rule is unit-testable. Self-defending: every byte of evidence is rendered inert (\xNN) so reading the report can never pwn the reader's terminal. 27-rule catalog in 9 families - CLIP (OSC 52 clipboard: CLIP01 write = CRITICAL silent clipboard hijack, CLIP02 query->stdin), LINK (OSC 8 hyperlinks: LINK01 label/target host mismatch, LINK02 dangerous scheme, LINK03 IP/userinfo/punycode authority), TITLE (TITLE01 window title, TITLE02 OSC 7 cwd, TITLE03 OSC 133/633 forged prompt marker), NOTIFY (NOTIFY01 desktop notification, NOTIFY02 BEL flood), ANSWER (answerback->stdin: ANSWER01 DSR CSI 5n/6n, ANSWER02 Device Attributes, ANSWER03 DECRQSS/ENQ, ANSWER04 XTVERSION/XTWINOPS), HIDE (content hiding: HIDE01 cursor-up line-jump overwrite = the exact Trail-of-Bits payload, HIDE02 mid-line CR, HIDE03 erase, HIDE04 alt-screen, HIDE05 conceal / fg==bg, HIDE06 hide cursor), INPUT (INPUT01 bracketed-paste disabled = pairs with a clipboard write, INPUT02 mouse/focus tracking, INPUT03 DECUDK key remap), DOS (DOS01 RIS/DECSTR reset, DOS02 oversized payload, DOS03 8-bit C1 introducer evasion), LAUNDER (LAUNDER01 base64/percent/backslash-encoded escape introducer, a second-order evasion) - severity-weighted score + grade + CI exit-code gate. Ships a benign reference capture that uses real SGR color, a legit matching-host OSC 8 hyperlink and CRLF endings yet scores 100/A (flags BEHAVIOR, not the mere presence of escape bytes) and a trojan capture with 17 findings across all 9 families incl. a CRITICAL OSC 52 clipboard hijack (base64 'rm -rf ~'), an 8-bit \x9b6n DSR that also trips DOS03, and a base64-laundered OSC 52 = LAUNDER01; 101 tests. Honest prior-art note in README/RESEARCH: the attack class is well-documented and generic ANSI strippers (strip-ansi), one MCP runtime gateway (tirith) and a server-side DAST (Bright) exist, but a read-only, LLM-free, pure-stdlib CLIENT-SIDE auditor/classifier over captured MCP session content with a severity score + CI gate did not. | [dev-experiment-024](https://github.com/freyesperales/dev-experiment-024) | per-repo |
| 025 | 2026-08-04 | herald - offline, LLM-free, dependency-free (pure-stdlib) trust & safety auditor for the A2A (Agent2Agent) AgentCard served at /.well-known/agent-card.json: the series' first entry on the agent-to-AGENT interoperability layer (experiments 001-024 all audit MCP / a static MCP-or-skills manifest / the MCP HTTP transport / terminal rendering). Audits the discovery & identity artifact a host agent fetches from a remote agent, caches, and injects VERBATIM into its delegation-planning LLM context - the exact mechanism Agent Card Poisoning (Keysight/SemiEngineering, Mar 2026) exploits. Clean split of observation (loader = the only filesystem module) from judgement (analyzer = a pure AgentCard->[Finding] transform with no I/O, clock or RNG) so a live-fetched card and a committed CI fixture grade through the identical path and every rule is unit-testable. 37-rule catalog in 7 families - TRUST (AgentCardSignature JWS/RFC7515 trust root: TRUST01 unsigned, TRUST02 alg:none forgeable = CRITICAL, TRUST03 symmetric HMAC alg, TRUST04 no key reference, TRUST05 malformed protected header, TRUST06 x5u/jku key-URL SSRF/off-origin = CRITICAL, TRUST07 served-but-unverified, TRUST08 provider/endpoint split identity), INJ (Agent Card Poisoning: INJ01 instruction-override text = CRITICAL, INJ02 external endpoint URL smuggled into prose, INJ03 imperative exfiltration phrasing, INJ04 template/variable-substitution syntax, INJ05 embedded tool-call/JSON-RPC snippet), PERC (perception & concealment: invisible/zero-width/TAG, bidi override, C0/C1 control, homoglyph/mixed-script identity impersonation, oversized context-bomb), SEC (security-scheme legality & credential hygiene: SEC01 literal embedded secret = CRITICAL, apiKey-in-query-string, HTTP Basic, cleartext/private OAuth endpoint, deprecated implicit flow, scope-less flow, exposed surface with no auth requirement), XPORT (endpoint transport & SSRF: cleartext service, XPORT02 private/link-local/cloud-metadata endpoint = CRITICAL SSRF/confused-deputy, dangerous scheme, userinfo authority, punycode/IDN host, transport-with-no-interface, unsafe ancillary URL), CONF (bad protocolVersion, schema legality, unknown transport token, ungated authenticated extended card) and LAUNDER (LAUNDER01 base64/percent/hex payload decoding to an injection signature or unsafe URL, a second-order evasion) - severity-weighted score + grade + CI exit-code gate, pure stdlib. Ships a clean reference card (signed ES256+kid, https everywhere, scoped OAuth authorization-code flow, no URLs/instructions in prose) that scores 100/A with ZERO findings and a trojan hotel-booking card (Cyrillic-homoglyph brand impersonation, IGNORE-ALL-PREVIOUS-INSTRUCTIONS + send-conversation-and-api_key-to-attacker payload concealed with zero-width/bidi/BEL, alg:none signature whose x5u key URL is the 169.254.169.254 cloud-metadata endpoint, cleartext link-local API interface, leaked AWS key + JWT, query-string API key, deprecated implicit flow, base64-laundered second exfil URL) that trips all 7 families and scores 0/F with 41 findings, 90 tests. Honest prior-art note: generic prompt-injection scanners, JWS/JOSE validators and SSRF URL classifiers exist independently; the wedge is a single A2A-schema-aware, offline, LLM-free, dependency-free auditor unifying signature-trust + card-poisoning + perception + scheme hygiene + endpoint SSRF + conformance + encoded-launder over one AgentCard with a severity score and CI gate. | [dev-experiment-025](https://github.com/freyesperales/dev-experiment-025) | per-repo |
| 026 | 2026-08-05 | recoil - offline, LLM-free, pure-stdlib auditor for the A2A push-notification / webhook callback channel (PushNotificationConfig register->deliver lifecycle): the series' first A2A RUNTIME entry (after #025 herald's static AgentCard) - webhook SSRF (metadata/private via literal-or-resolved IP), register-vs-deliver DNS rebinding, transport hygiene, receiver caller-auth, the spec's signed-JWT delivery contract (alg:none/jku-SSRF/missing-claims/aud+taskId binding/jti replay), lifecycle amplification & delivered-content injection - 34 rules / 8 families, severity score + CI gate; clean 100/A + trojan 0/F (all 8 families, 7 CRITICALs), 97 tests | [dev-experiment-026](https://github.com/freyesperales/dev-experiment-026) | per-repo |
| 027 | 2026-08-07 | vitrine - offline, LLM-free, dependency-free (pure-stdlib) static auditor for MCP Apps (SEP-1865, spec 2026-01-26) UI resource bundles: the series' first entry on the CLIENT-SIDE RENDERING / web-application layer - every prior entry audits a JSON message channel (011-022), a static manifest (001-010), the HTTP transport (#023 rampart), terminal rendering (#024 escapement) or A2A (#025 herald, #026 recoil); vitrine audits the actual HTML+JavaScript a server ships for the host to render *inside the assistant's own surface*. The wedge is relational: MCP Apps is the one MCP surface where the server DECLARES its capability envelope (_meta.ui.csp connect/resource/frame/baseUriDomains, permissions, visibility, prefersBorder) and separately SHIPS the code, and the spec makes the host's policy a pure function of that declaration ('Host MAY further restrict but MUST NOT allow undeclared domains') - so a declared-vs-observed diff is a conformance check, not a heuristic. Grounded in the fact that a View is not a page but an MCP CLIENT holding a privileged bridge (tools/call, resources/read, ui/update-model-context, ui/message, ui/open-link): markup injection here inherits authority to act as the user, and Backslash's analysis of the 2026-07-28 spec notes these attacks 'operate entirely on the endpoint... None of this touches the network', so MCP gateway/proxy tooling is structurally blind to them. 42 rules / 6 families - DECL (ui:// scheme, fixed text/html;profile=mcp-app MIME, dangling/orphan resources, visibility+permission legality, self-supplied meta CSP), CSP (two-way egress diff over markup+CSS+script origins: undeclared reach AND declared-but-unused excess, wildcard/scheme-only/cleartext/wildcard-subdomain/base-uri), EXFIL (tool-result data reaching a network call, capability API without its permission, cross-origin form post, runtime-computed destination), BRIDGE (postMessage '*' targetOrigin, listener without event.origin, reserved ui/notifications/sandbox-* spoofing, ui/update-model-context fed untrusted data = prompt injection from inside the sandbox, agent-driving without a user gesture, parent.document frame escape), SINK (innerHTML/eval/string-timer fed tool data, weighted by the bridge the injected code inherits) and FACADE (credential fields, host-chrome mimicry = the documented VS Code auth-prompt UI-mimicry attack, inheritance-aware invisible instruction text, full-viewport clickjack overlay, nested remote iframe). Technical core: it LEXES JavaScript rather than grepping it - comments, all three string forms, template literals with recursive interpolation and regex literals (disambiguated from division) are blanked offset-for-offset so every line number stays true and a token inside a comment or string is never a call, while URLs and bridge method names are recovered exactly from a side table. Severity score + grade + CI exit-code gate; ships a conformant bundle (0 findings, 100/A - the catalog's false-positive gate) and a hostile bundle with 41 documented ground-truth defects across all six families incl. 6 CRITICALs (sandbox-proxy spoofing, parent.document escape, wildcard connect domain, tool-result exfiltration, eval of tool data, credential harvesting), 221 tests | [dev-experiment-027](https://github.com/freyesperales/dev-experiment-027) | per-repo |
| 028 | 2026-08-08 | penumbra - deterministic cache-coherence model checker for the MCP 2026-07-28 caching model (SEP-2549 ttlMs/cacheScope + listChanged): the series' first TIME-based entry and first simulator. Finds phantom/eclipsed/drifted tools and stale-authority auto-approvals in the interval between two fetches (33 rules, 7 families, counterexample traces) | [dev-experiment-028](https://github.com/freyesperales/dev-experiment-028) | per-repo |
| 029 | 2026-08-10 | contrail - offline, LLM-free, dependency-free (pure-stdlib) BIDIRECTIONAL auditor for the MCP TRACE-CONTEXT / TELEMETRY PROPAGATION PLANE (SEP-414, Final 2026-07-28: _meta.traceparent / _meta.tracestate / _meta.baggage): the series first entry on the OBSERVABILITY plane. Every prior entry audits the PAYLOAD - a static manifest (001-010), one message channel (011-022), the HTTP transport (#023 rampart), terminal rendering (#024 escapement), A2A (#025 herald, #026 recoil), an MCP Apps UI bundle (#027 vitrine) or the caching timeline (#028 penumbra) - and asks is this document/message/bundle safe? contrail audits the METADATA ENVELOPE that rides alongside every single request, and asks a directional question instead: what is leaving, and what am I accepting? The wedge: SEP-414 makes these three the ONLY keys in _meta exempt from reverse-DNS namespacing, they are attached automatically by the tracing SDK and propagated to every peer, and the SEPs entire Security Implications section is two sentences (trace context may include correlation IDs; follow existing data-handling guidance) - while the two W3C specs it incorporates are far sharper (W3C Baggage: application owners MUST ensure no proprietary information crosses trust boundaries via baggage; W3C Trace Context: forged trace headers enable DoS and tracestate reveals your monitoring stack), and an MCP server IS a trust-boundary crossing by construction. Grounded in a shipped 2026 CVE class: the baggage extract path had no item/byte limits in dd-trace-py/-js/-go/-rb/-dotnet (CVE-2026-50271..50276) and opentelemetry-rust (GHSA-w9wp-h8wv-79jx). Trust is a DECLARED input, not a guess (first-party / third-party / unknown), because the same bag is correct hygiene to an internal gateway and a critical leak to a vendor. Clean split of observation from judgement mirroring #023/#026: loader.py is the only filesystem module, everything downstream is a pure Capture->[Finding] transform with no I/O, clock or RNG, so a committed fixture and a live capture grade identically and all 42 rules are unit-testable. Self-defending like #024: every evidence byte is rendered inert. 42 rules / 7 families - LEAK (egress classification across the boundary: LEAK01 credential-shaped value = CRITICAL, PII, internal host, path disclosure, tenant id to a third party, high-entropy token, LEAK07 base64/hex/percent laundered secret, secret parked in a tracestate vendor entry), BOUND (propagation-scope policy rather than value sensitivity: baggage reaching a third party unscrubbed, byte-identical bag broadcast across trust classes, unclassified peer, cleartext, accretion, BOUND06 peer-planted member relayed to another peer = the telemetry confused deputy), PARSE (W3C conformance both directions: malformed traceparent, all-zero ids, forbidden ff / future-version 55-char rule, 32-member tracestate cap, grammar violations, baggage-octet and bare-percent violations, duplicate keys), DOS (the CVE class, ingest-only: 64-member and 8192-byte propagation limits, token bomb, percent-decode amplification / nested list bomb, 512-char tracestate budget), GRAFT (trace integrity - a callee CONTINUING your trace is correct and never flagged: GRAFT01 untrusted peer substitutes its own trace root, first-party orphaned span, forced sampled bit = billing DoS, trace-id reused across principals or sessions, low-entropy/counter-like trace-id), INJECT (the channel reaches high-trust destinations while being reviewed by nothing: INJECT01 CR/LF where SEP-2028 forwards _meta into outbound HTTP headers = header injection CRITICAL and the catalogs one posture-conditional rule, instruction-override text inbound incl. base64-decoded, invisible/bidi/TAG Unicode, ANSI/C0 log forging, structured-log delimiter breakout, metadata/SSRF-shaped URL) and CORR (privacy: PII used as a KEY - indexed even when values are redacted, tracestate naming your observability vendors to a third party, two principals commingled in one propagated context, topology disclosure) - severity-weighted score + grade + CI exit-code gate. Ships a clean reference capture that is deliberately NOT trivial (real baggage, two-vendor tracestate, one trace continued across an internal gateway AND a live third-party peer) scoring 100/A with ZERO findings purely because the client scrubs baggage+tracestate at the boundary and sends the third party only traceparent - a test asserts that property directly so the fixture cannot go clean for the wrong reason - and a hostile capture scoring 0/F with 81 findings across all 7 families incl. 9 CRITICALs, generated by a committed build_hostile.py whose output a test re-derives and diffs; another test FAILS if any of the 42 rules is not exercised by it. 336 tests, weighted toward negative cases (a UUID must not read as an opaque token; a callee continuing your trace must not read as grafting). Honest prior-art note: secret/PII/SSRF classifiers, JOSE validators and W3C parsers all exist independently, the baggage DoS is a documented CVE class, the trust-boundary rule is in the W3C spec itself, and OWASPs MCP08:2025 telemetry entry covers the OPPOSITE problem (telemetry being absent); the wedge is the assembly - a single MCP-_meta-aware, trust-boundary-typed, bidirectional auditor over one captured session. Stated limitations: trust classification is an input not a discovery, it confirms rather than searches, classifiers are pattern-based, and no SDK capture shim ships here | [dev-experiment-029](https://github.com/freyesperales/dev-experiment-029) | per-repo |
| 030 | 2026-08-11 | janus - offline, LLM-free, dependency-free (pure-stdlib) DIFFERENTIAL auditor for the MCP HEADER-MIRRORING PLANE (SEP-2243, Final; MCP 2026-07-28): the series' first entry on the plane where the protocol says the same thing TWICE. Every prior entry audits one representation - a static manifest (001-010), one message channel (011-022), the HTTP transport hardening (#023 rampart), terminal rendering (#024 escapement), A2A (#025 herald, #026 recoil), an MCP Apps bundle (#027 vitrine), the caching timeline (#028 penumbra) or the telemetry envelope (#029 contrail) - and asks 'is THIS artifact safe?'. janus asks an AGREEMENT question instead: two components both read this one request, do they read the same thing? The wedge: 2026-07-28 mirrors body values into HTTP headers (Mcp-Method<-method, Mcp-Name<-params.name|uri, and x-mcp-header promoting chosen TOOL-CALL ARGUMENTS into Mcp-Param-{Name}) precisely so gateways, WAFs and load balancers can route, meter and AUTHORIZE without parsing the body - the textbook precondition for a request-smuggling-shaped defect. The spec's only defence is one MUST bound to the LAST component in the chain (servers that process the body MUST reject header/body mismatches, 400 + -32020 HeaderMismatch), protecting a decision the FIRST component already made; and SEP-2243 itself admits header values 'originate from tool call arguments, which may be influenced by an LLM' and MUST NOT be trusted for security decisions - a prohibition stated in prose and enforced by nothing. Reconstructs BOTH readings of each captured request (HeaderView = what a body-blind intermediary believes, BodyView = what the origin will execute) and takes the trust TOPOLOGY as a declared input, so severity escalates only when a security decision actually keys on the header that diverged (the same Mcp-Name mismatch is a curiosity behind a dumb load balancer and an authz bypass behind a tool-allowlisting gateway). Ships the sentinel codec TWICE on purpose - read_strict (spec-exact) and read_lenient (the forgiving decoder real proxies ship: case-insensitive markers, repaired base64 padding) - so their disagreement IS the finding. Observation split from judgement like #023/#029: loader.py is the only filesystem module, everything downstream is a pure Capture->[Finding] transform with no I/O, clock or RNG, asserted STRUCTURALLY by a test that parses each pure module's imports. Self-defending like #024: every evidence byte rendered inert. 60 rules / 7 families - MIRROR (plane divergence: method/name/param/version, MIRROR05 divergence CONCEALED inside a base64 sentinel = invisible to any byte-comparing intermediary, case-only, numeric-only, MIRROR08 whitespace-only as an INFO false-positive gate since the spec requires the server to ACCEPT it, ambiguous name|uri source), SMUGGLE (parser differentials: duplicate Mcp-Method/Mcp-Name/Mcp-Param with differing values = first-wins vs last-wins, differing name spellings, raw CR/LF/NUL, unencoded non-ASCII, unencoded edge whitespace, malformed base64 that strict rejects and lenient recovers, SMUGGLE09 non-lowercase =?BASE64? markers = a literal to the spec but decodable to a lenient reader, SMUGGLE10 unencoded sentinel-shaped literal), ENFORCE (did the origin do the MUST: ENFORCE01 mismatch ACCEPTED = CRITICAL, the single omission that voids every upstream policy at once, missing required header accepted, wrong code/status, ENFORCE05 the stale -32001 that PR #2907 renumbered to -32020, illegal chars accepted, undecodable sentinel accepted, rejection message reflecting raw attacker bytes), XPARAM (annotation integrity incl. a real STATIC-REACHABILITY walker over JSON Schema - the chain must consist solely of 'properties' keys and may not pass through items/composition/conditional/$ref - plus empty/tchar/control names, case-insensitive collisions, non-primitive and number types, JS-unsafe integer bounds, unbounded strings, and XPARAM01 an UNCONSTRAINED MODEL-CONTROLLED parameter feeding a policy-bearing header), POLICY (topology soundness: POLICY01 a header-based policy with NO validating component anywhere = CRITICAL, no protocol-version gate, POLICY03 a security decision keyed on an LLM-sourced Mcp-Param = CRITICAL, meter/quota attributed to the wrong subject, stripping unrecognised Mcp-Param, latent no-validator topology), LEAK (the mirroring plane as EGRESS into every proxy access log: credential-shaped value, PII, local path in Mcp-Name, LEAK04 a secret wrapped in base64 = no confidentiality gained but log secret-scanners now miss it, oversized value, high-entropy token) and CONF (Accept, required headers, orphan/omitted/phantom Mcp-Param, legacy Mcp-Session-Id|Last-Event-ID) - severity-weighted score + grade + CI exit-code gate (0 clean / 1 findings / 2 unreadable input). Ships a conformant capture that is deliberately NOT trivial (mirrors a parameter, legitimately base64-encodes a non-ASCII argument, routes on Mcp-Method AND authorizes on Mcp-Param-Region) yet scores 100/A purely because the origin validates and the gateway re-derives the region decision from the authenticated principal - two tests assert those properties directly and two more flip each one and assert it then FAILS, so the fixture cannot go green for the wrong reason - and a hostile capture scoring 0/F with 80 findings across all 7 families incl. 19 CRITICALs, generated by a committed build_hostile.py whose output a test re-derives and diffs; another test FAILS if any of the 60 rules is unexercised by the fixtures. 442 tests, weighted toward negative cases (a legitimate sentinel must not read as smuggling; identical duplicate headers are not a differential; a whitespace-only difference must not oblige rejection; Mcp-Name must never be entropy-scored because its content is dictated by the spec). Honest prior-art note: differential analysis of two HTTP parsers is mature (HTTP Garden arXiv:2405.17737) and the bug class shipped as 2026 CVEs (@hapi/content CVE-2026-44974 duplicate-parameter smuggling, python-hyper/h2 CVE-2026-71554 duplicate Host, Go HTTP/2 CVE-2026-2219), while header/body consistency is a MUST in the spec itself; the wedge is the assembly - MCP-aware, topology-typed, two-readers-of-one-request, plus a static x-mcp-header graph audit and an egress view. Stated limitations: topology is a declared input not a discovery, it confirms rather than searches, classifiers are pattern-based, the lenient reader is a model not a census, and no capture shim ships here | [dev-experiment-030](https://github.com/freyesperales/dev-experiment-030) | per-repo |
| 031 | 2026-08-12 | waterline - offline, LLM-free, dependency-free (pure-stdlib) DIFFERENTIAL REACHABILITY analyzer for Agent Skill bundles: treats reachability as a property of the RESOLVER, not of the artifact - computes the closure under 4 resolver models over one shared candidate set and reports agent MINUS containment = BLIND SPOTS (the audit-runtime gap localized to specific files) and containment MINUS (link UNION directive) = REFERENCE LAUNDERING; plus tiered disclosure model, capability-by-tier gap, confinement escapes, dark reads, cycles - 14 codes, score/grade/CI gate, 118 tests | [dev-experiment-031](https://github.com/freyesperales/dev-experiment-031) | per-repo |
| 032 | 2026-08-13 | warrant - offline, LLM-free, dependency-free (pure-stdlib) static auditor for the AGENT PERMISSION-POLICY / CONSENT plane: the allow/ask/deny rule set a coding agent consults BEFORE it runs a tool call. Every prior entry audits what the other side says - a static manifest (001-010), one MCP channel (011-022), the HTTP transport (#023), terminal rendering (#024), A2A (#025-026), an MCP Apps bundle (#027), the caching timeline (#028), the telemetry envelope (#029), header mirroring (#030) or a Skill's reachable closure (#031) - and asks 'is this artifact safe?'. warrant audits what the CLIENT HAS ALREADY AGREED TO, and asks an admitted-language question instead: a permission policy is a pattern language, so what is the full set of tool calls it admits, and does that set intersect the effects its author was trying to forbid? Three ideas separate it from the syntactic linters that exist (cclint, browser-based permission builders): (1) CONSTRUCTIVE VERIFIED WITNESSES - it never says 'this rule looks broad'; it synthesizes a concrete command, re-evaluates it through its own matcher models, and emits it ONLY if a model auto-approves it, so the tool cannot claim a bypass it has not demonstrated and every witness is reproducible by construction; (2) EFFECT-EQUIVALENCE, not program names - programs are catalogued by the effects they produce and a generality tier, so deny Bash(curl:*) + allow Bash(python:*) is provably NOT a network deny, mechanizing the field's sharpest observation that command allowlists are the wrong abstraction layer because you name the program when you care about the effect and any code-runner collapses the distinction; (3) MATCHER DIFFERENTIAL where DISAGREEMENT IS THE FINDING - the same policy is evaluated under strict (segments AND substitutions; sound), segment (operators only, blind to $(...); the documented behaviour) and head (whole line as one unit; the SHIPPED DEFECT), so a policy safe under strict but dangerous under head is one whose safety depends on an implementation detail its author never chose, and each witness reports WHICH models admit it. Grounded in shipped defects: claude-code #28784 (Bash(cd:*) admits cd /p && python x), #36637 (Bash(git:*) admits git status && rm -rf past an explicit deny, unlogged), the $(...) substitution bypass, CVE-2025-54795 argument injection, and GhostApproval/SymJack (CWE-61 + CWE-451, six coding agents, approval prompt showing a different path from the write target). 23 finding codes in 5 families - FORM (inert/ignored/subsumed/contradictory/unknown-tool rules, incl. the Bash(command:...) shape the runtime silently discards), ADMIT (universal grant, shell-equivalent grant via argument injection, substitution escape, chain escape, redirection escape, pinned-but-config-driven runner), SHADOW (denied effect still reachable, deny defeated by re-spelling /bin/rm or command rm, runner inventory), PATH (the single-slash trap where /path is settings-dir-relative NOT absolute, overbroad anchors, sensitive-target sweep, anchor escapes, writes outside the workspace), STACK (same rule text resolving differently per settings file, unreviewed settings.local.json widening the reviewed policy, bypassPermissions, acceptEdits with no write bound). Clean observation/judgement split: loader.py is the only filesystem module, analyzer.py is a pure Policy->Report transform with no I/O, clock or RNG, and the resolution context (home, cwd) is a DECLARED Environment rather than a discovered one, so a committed fixture and a live settings stack grade through identical code on any machine. Self-limiting by design: unknown programs model as benign so it under-reports rather than inventing findings, path analysis is purely lexical (no symlink resolution), and WT03 states outright that findings UNDERSTATE exposure under bypassPermissions. Witnesses are inert - execution proved with id, every network payload targets the reserved .invalid TLD. 318 tests incl. direct reproductions of #28784 and #36637, 6 fixture policy stacks (hardened grades 100/A as the control), severity-weighted score + grade + CI exit-code gate | [dev-experiment-032](https://github.com/freyesperales/dev-experiment-032) | per-repo |
| 033 | 2026-08-17 | revenant - offline, LLM-free, dependency-free (pure-stdlib) ADOPTION-SURFACE auditor for the AGENT PERSISTENT-MEMORY plane: the store the agent writes for its own future self. The series' first entry on state the agent AUTHORS rather than state it receives - every prior entry audits an inbound or declared artifact (a static manifest 001-010, one MCP channel 011-022, the HTTP transport #023, terminal rendering #024, A2A #025-026, an MCP Apps bundle #027, the caching timeline #028, the telemetry envelope #029, header mirroring #030, a Skill's reachable closure #031, the permission policy #032) and asks 'is this artifact safe?'. revenant asks a question no content scanner asks: GIVEN that a stored memory is bad, DOES IT MATTER - will it ever be in the room, in what future session, at what authority, and what is the minimum edit that keeps it out? Motivated by a measured gap: MemSecBench (arXiv 2607.27080) reports memory poisoning PERSISTS 84.2% of the time (MPSR) but reaches end-to-end consequence only 50.3% (E2E-ASR), concluding that 'persistence is permissive; ADOPTION constitutes the decisive filtering boundary' - and every shipping defence (Ratine, OWASP Agent Memory Guard, NVIDIA SkillSpector, Cisco Watchdog) scores the WRITE. That 34-point adoption gap is unmeasured, and OWASP ASI06 (Memory and Context Poisoning, 2026 Agentic AI Top 10) names TEMPORAL DECOUPLING as the distinguishing property - exactly what a content classifier cannot see and a reachability model can. Three ideas carry it: (1) a RECALL TIER LATTICE ordered by adoption probability rather than severity - ambient (text in the always-loaded index, no gate at all) > indexed (advertised every session) > PROMOTED (not advertised but transitively reachable by [[wikilink]] from something that is, so a hidden memory rides into context behind a legitimate one - the amplification tier, and a property of the GRAPH BETWEEN items that is invisible to every per-item scanner) > gated > orphan, with the exact promotion path reported as the witness; (2) BOUNDED rather than FAKED retrieval - real recall is an embedding or an LLM judgement and neither is reproducible offline, so instead of a dishonest point estimate it computes a BAND (strict lexical = likely recalled, permissive = could be) and grades on the permissive bound because that is the one an attacker picks, with a deliberately bland description matching every probe since vagueness is how a poisoned item stays plausible to a reviewer while staying retrievable; (3) an EXACT MINIMUM-EDIT quarantine plan - severing hazards from ambient reach is a minimum edge cut over index pointers and wikilinks, solved with Edmonds-Karp max-flow rather than a greedy pass, because memory stores are hand-curated and a plan demanding eleven deletions when two suffice gets ignored and the store stays poisoned (the fixture: 4 hazards, 2 edits). 13 rules, EVERY ONE a JOINT content-AND-reach condition so content alone never yields a high severity and reach alone never yields a finding: REV001 binding directive in the always-loaded index, REV002 link promotion, REV003 undeclared payload (description shares no topic vocabulary with the directive - unannounced at BOTH the retrieval and the human review boundary, since the description is simultaneously the retrieval key and the only thing an index reader sees), REV004 scope inflation, REV005 AUTHORITY LAUNDERING (type: user/feedback asserts 'the operator told me this' - the highest trust the format can express - while the body carries third-party origin markers, so a web page is re-read as an operator instruction; motivated by arXiv 2606.24322 on origin-bound authority), REV006 descriptive type carrying a binding directive, REV007 DORMANT TRIGGER (directive deferred to 'when deploying' / 'when handling payments' - timed to the moment oversight is thinnest; arXiv 2605.28201 plant-persist-trigger), REV008 exfil sink, REV009 credential at rest, REV010 destructive directive, REV011 policy override, REV012 DANGLING PROMOTION (a link to a memory that does not exist yet is a RESERVED SEAT - whatever is written under that name later inherits ambient reach with nobody re-reviewing the linking text), REV013 orphan. Severity is ADOPTION-WEIGHTED: the identical webhook-exfiltration directive scores 80.0/critical when indexed and 10.0/low when unreachable, where a content scanner returns the same verdict for both. Honest about its own seams: structural reach (tiers, promotion paths, dangling links, the min-cut) is EXACT graph fact, while force/payload/gated-vs-orphan are heuristics; the quarantine plan states outright that cutting structure does NOT make an item unretrievable and lists what stays reachable; and it does not compete with the content scanners it names - it is the complement, deliberately shallow on payload lexicons. Two adapters (index-plus-notes markdown, flat JSON/vector export), sentence-segmented directive classification so a paragraph-in-one-string vector record does not hide its instruction after the first full stop, pointer-line suppression so a well-formed index titled 'Deploy runbook' is not read as the imperative 'deploy', and a lexicon confined to ONE reviewable file so every claim traces to a listed phrase. 141 tests, clean + poisoned reference stores with 21 documented (rule, item) ground-truth pairs asserted in BOTH directions (a new false positive fails as loudly as a missed detection), the clean store deliberately containing a legitimately BINDING memory that must stay silent; exposure score + grade + CI exit-code gate | [dev-experiment-033](https://github.com/freyesperales/dev-experiment-033) | per-repo |
| 034 | 2026-08-18 | satrap - offline, LLM-free, dependency-free (pure-stdlib) static auditor for the AGENT DELEGATION PLANE: two-axis authority lattice (capabilities x consent posture) under the product order + combined edge/authority fixpoint over the delegation graph + return-channel laundering analysis; 25 checks in 5 families, witness-replayed findings, scored report + CI gate | [dev-experiment-034](https://github.com/freyesperales/dev-experiment-034) | per-repo |
| 035 | 2026-08-19 | deadlatch - static auditor for the AGENT HOOK PLANE (the shell commands wired to the tool-call lifecycle): finds gates that look like gates and do not gate - denial paths exiting 1 instead of 2, exit 2 on the 17 events that discard it, glob-shaped matchers silently compiled as regex, capability classes left half-covered. 49 rules, pure-stdlib, never executes the hooks it audits | [dev-experiment-035](https://github.com/freyesperales/dev-experiment-035) | per-repo |
| 036 | 2026-08-20 | sluice - static auditor for the AGENT NETWORK-EGRESS PLANE (the allow/deny domain lists deciding where an agent's bytes may go): the allowlist grants the suffix and the attacker rents the leaf. Derives its flagship rule from the Public Suffix List's private section - a registry of namespaces whose leaves are rented - so *.amazonaws.com and *.com are the same finding at different severities. 57 rules, 5 modelled runtimes so `diff` shows one config confining differently depending on who loads it, pure-stdlib, never opens a connection | [dev-experiment-036](https://github.com/freyesperales/dev-experiment-036) | per-repo |
| 037 | 2026-08-24 | entrepot - offline, LLM-free, dependency-free (pure-stdlib) EMERGENT-RISK auditor for the MCP AGGREGATION/FEDERATION PLANE: the first entry whose unit of analysis is the composition rather than a component. Replays the merge bottom-up and reports what the aggregation boundary destroyed (shadowed names, laundered annotations, erased provenance, flattened identity) plus the lethal trifecta assembled across upstreams none of which carries all three legs alone | [dev-experiment-037](https://github.com/freyesperales/dev-experiment-037) | per-repo |
| 038 | 2026-08-25 | reprise - offline, LLM-free, dependency-free (pure-stdlib) RE-EXECUTION SAFETY MODEL CHECKER for agent tool stacks: the series first entry on the RETRY / RE-DISPATCH plane. Reads a declarative retry profile (tool manifest + nested retry layers + idempotency-key derivation + Tasks retention TTL) and proves, per (tool x fault class), how many times an external side effect can actually COMMIT - splitting faults into DEFINITIVE (never dispatched, retry is free) vs AMBIGUOUS (dispatched, outcome unknown, retry duplicates), multiplying nested attempt budgets (3x3x3 = 27 dispatches, not 9), and evaluating idempotency-key survival PER LAYER BOUNDARY: a content-derived key stops a transport replay but not an agent replan loop that re-derives its arguments, so dedup is not on or off but on at some boundaries and off at others. Grades key sources STABLE/VOLATILE/BUCKETED/SUSPECT to catch schemes that pass code review and are inert at runtime (uuid4(), now(), attempt counters, refreshed payload fields), flags under-specified keys whose collisions silently DROP a genuine call, models the MCP MRTR resultType input_required re-send as a pseudo-layer that no retry budget caps (zero configured retries, effect still commits twice), and catches retry budgets that outlive Tasks result retention so a committed effect is orphaned. 14 rules (RPZ001-014), an attempt-by-attempt counterexample schedule per finding, severity + commit-budget CI gates, 135 tests. | https://github.com/freyesperales/dev-experiment-038 | per-repo |
| 039 | 2026-08-27 | halflife - offline, LLM-free, dependency-free (pure-stdlib) RESIDENCY MODEL CHECKER for the AGENT CONTEXT-COMPACTION plane: the series' first entry whose subject is not an artifact but the WINDOW OF TIME an artifact is in force. Every prior entry audits something PRESENT - a static manifest (001-010), one MCP channel (011-022), the HTTP transport (#023), terminal rendering (#024), A2A (#025-026), an MCP Apps bundle (#027), the caching timeline (#028), the telemetry envelope (#029), header mirroring (#030), a Skill's reachable closure (#031), the permission policy (#032), the memory store (#033), the delegation graph (#034), the hook plane (#035), the egress list (#036), a federation merge (#037), a retry profile (#038) - and every one of them silently assumes the artifact is STILL IN THE ROOM when it matters. halflife audits that assumption. A governance constraint binds an agent only while it is RESIDENT in the context window; a compaction policy is a schedule for removing things from that window; therefore the operative question is not 'is this policy well written' but 'is it still there at turn 47, which compaction event took it, and what fired afterwards'. Grounded in a measured number rather than an intuition: ConstraintRot (arXiv 2606.22528, 1,323 episodes x 7 model families) reports policy violation at 0% with the policy in full context, 30% pooled after compaction (59% worst model), and - the decisive split - 0% when the constraint SURVIVES the summary against 38% when it is DROPPED, concluding that survival, not content, is the filtering boundary. Survival is not semantic, it is SCHEDULING, and a declared token profile plus a declared compaction policy makes it exactly decidable offline with no model call. Three ideas carry it: (1) SEVERITY INVERTED BY FORCE - the same paper measures soft organisational policy decaying +50 points under compaction against +6 for hard safety norms, an 8.3x gap, because a model re-derives 'do not exfiltrate an SSH key' from its weights with the policy absent and re-derives NOTHING about 'vendor email goes through legal' or 'deploy only to eu-west-1', so halflife grades an evicted SOFT constraint CRITICAL and an evicted HARD one HIGH - the opposite of every severity-by-topic scanner, and the correct ordering for this plane; (2) TRIGGER STEERING (HL-EVICT003), the observation that compaction fires on a TOKEN THRESHOLD, so whoever controls the SIZE of what the agent ingests controls WHEN it fires, and residency is a function of that phase - a constraint comfortably resident at a call today is made non-resident by PADDING an earlier untrusted turn, with no instruction injected and no content filter tripped, the fetched page merely bigger; every witness is found by ladder search and then RE-SIMULATED before emission, so an unproven steering claim is never reported (the shipped example: pad turn 8 by 31,331 tokens, one new compaction event lands between the policy and the call, C-004 goes RESIDENT -> SUMMARIZER_DEPENDENT at the spend.commit on turn 16); (3) A REPAIR THAT REPLAYS ITSELF - the paper's defence is constraint pinning (0% violation restored across all seven models at ~47 tokens), real harnesses give a BOUNDED pinning region, so 'which constraints do I pin' is an exact 0/1 knapsack solved by DP rather than greedily (a plan demanding eleven pins when three suffice gets ignored and the session stays ungoverned) - but pinned tokens are RESIDENT FROM TURN ZERO and push the window toward the trigger sooner, so a pin plan can OPEN a gap for a constraint it did not select, which the '<0.5% overhead' framing hides and which halflife reports by re-simulating the plan it just computed. Residency is a THREE-state lattice and the third state is an admission: RESIDENT, EVICTED (structurally deleted - CERTAIN), and SUMMARIZER_DEPENDENT (handed to a model-authored summary, whose contents halflife REFUSES to predict, grading on the pessimistic bound because that is the bound an adversary selects for and because 0% and 38% are not close enough to average) - with TRANSCODING DEPTH, the exact count of summarisation passes traversed, reported alongside, since at depth >=2 the window holds a paraphrase of a paraphrase that no author reviewed. 24 rules in 6 families: RES (the flagship governance gap, evicted vs summariser-dependent, transcoding depth, never-resident ordering defects, orphan constraints, ungoverned effectors), CHAN (placement - the constraint written in a channel this harness compacts while it preserves another, untrusted-origin policy, a policy whose only carrier is a tool result under a tier that clears tool results on a schedule, split governance where half a policy outlives the rest and reads as the whole), EVICT (the Compaction-Eviction Attack shape from the same paper - summariser-directed text co-resident with a constraint the same event hands to the same summariser - plus supersession claims and trigger steering), LAUNDER (the summarisation boundary destroys channel labels, so content that entered tagged as data leaves as narration in the trusted prefix; and the tier race where the cheap tier DELETES a tool result after the expensive tier already absorbed it, leaving a conclusion whose source cannot be re-checked), CFG (the shipped `instructions` parameter COMPLETELY REPLACES the default summarisation prompt rather than extending it, so a string naming three things to keep is also a string that stopped asking for everything else; pause_after_compaction configured with no inspection step; triggers outside documented bounds; a policy that cannot reduce the window), PIN. Seven harness profiles each carrying a citable provenance string printed by `halflife profiles` - api-compaction and api-compaction-tuned (Messages API compact_20260112, trigger 150000, minimum 50000, the tuned variant configured exactly as the documentation's own examples configure it), coding-agent (the three tiers: microcompact clearing stale tool results with NO model call, full compact, session-memory compact), sliding-window (the honest worst case, where eviction is CERTAIN rather than summariser-dependent), structured-eviction (CWL, arXiv 2606.11213), event-log (Scroll, arXiv 2608.21690, modelling recoverable eviction - and halflife still reports the gap, because recoverability is not residency and a constraint the agent must CHOOSE to look up does not bind the choice), pinned - which makes the headline demo a DIFFERENTIAL: one session, seven compaction policies, seven different governance outcomes, nobody having edited a line. Ships two worked sessions with IDENTICAL token profiles, turn counts, channels and call sequences, differing only in WHERE each constraint was written: examples/decaying is a session a careful engineer ships and a reviewer approves (31 findings under api-compaction, nothing in it careless) and examples/durable is the same workload placed well - not shorter, PLACED DIFFERENTLY - which grades 100/A with ZERO findings on the two profiles that re-inject the channels it used and fails on the rest, because policy placement is only durable RELATIVE TO A HARNESS. Two results fell out that the design did not anticipate: the LLM-free tool-result-clearing tier PRESERVES governance markedly better than summarisation does, because the thing eating the window is tool output rather than policy; and durable under the event-log profile has zero findings under replay yet five CRITICALs from trigger steering alone - clean only for as long as nobody chooses the size of what the agent reads. Same observation/judgement split as #023 rampart, #035 deadlatch and #036 sluice: loader.py is the ONLY module that opens a file, every judgement module is a pure transform, and test_architecture.py walks each module's AST and fails the build if a judgement module imports os/pathlib/time/random/socket/subprocess or calls open() - the same walk enforcing the harder invariant that NOTHING in the package imports a network module or a model SDK, since auditing governance decay by shipping the pre-compaction transcript to a model would be a strange way to protect a session; and the guard is itself tested against synthetic bad source, because a guard that cannot fail is decoration. A live transcript instrumenter was rejected ON THE MERITS, not on effort: it answers 'did THIS summary drop the policy' (one sample of a stochastic process) rather than 'does this CONFIGURATION hand the policy to a summariser at all' (a property of the system), it cannot be re-derived in CI six months later, and it requires the transcript to leave the machine. Two design corrections are visible in the rules: HL-CFG004 originally fired on the mere PRESENCE of a hard constraint, penalising a session that had correctly written it into a preserved channel and recommending a pinning region to an author who no longer needed one, and now fires only for hard constraints that actually degrade; and the pin mechanism was silently defeated by re-assertion, selecting five constraints, spending 167 tokens and closing ZERO witnesses while reporting a plan - caught only because the planner verifies by replay instead of trusting itself, which is the entire argument for building it that way. scan via audit/simulate/diff/pin/explain/rules/profiles CLI, text+JSON+SARIF output, severity-weighted score + letter grade + CI exit-code gate, lexicon confined to ONE reviewable file of 36 phrases with every textual finding quoting its match in context, 217 tests green under both pytest and unittest, zero runtime dependencies. | https://github.com/freyesperales/dev-experiment-039 | per-repo |
| 040 | 2026-08-28 | sleight - path denotation auditor for the AGENT WORKSPACE-CONFINEMENT plane: a confinement rule is a predicate over path STRINGS, the filesystem is a many-to-one unstable NAMING FUNCTION. Computes the three functions that should agree - judge (policy verdict), show (approval prompt), open (kernel) - and reports every name where they disagree, which is what every 2026 disclosure here actually is. 50 rules, exact minimum remediation cut PLUS the escapes no cut can close (hardlinks, 8.3 names, case folds), 5 runtimes so `diff` shows one repo confining differently depending on who opens it. Pure-stdlib, never opens a file it is judging | [dev-experiment-040](https://github.com/freyesperales/dev-experiment-040) | per-repo |
| 041 | 2026-08-31 | parallax - projection auditor for the MCP TOOL-RESULT plane: a tools/call result is a broadcast to two readers with different parsers - the human who approves and the model that acts - and audience annotations, the unenforced content/structuredContent equivalence, and client render caps each let a server hand them different things. Computes both projections under 6 client profiles and reports every divergence, including the case where honouring the spec is what splits the readers. 48 rules, text+JSON+SARIF, score + CI gate + remediation plan, 353 tests, pure-stdlib | [dev-experiment-041](https://github.com/freyesperales/dev-experiment-041) | per-repo |
| 042 | 2026-09-01 | undertow - offline, LLM-free, dependency-free (pure-stdlib) RESIDUE & DUPLICATION AUDITOR for the MCP CANCELLATION / ABORT plane: the series' first entry on the STOP leg. Experiments 001-041 all audit an agent going FORWARD - what it may call, read, remember, spawn, retry or return. undertow audits what happens when someone pulls the plug. The thesis is a contradiction inside a single spec revision: MCP 2026-07-28 made cancellation IMPLICIT on Streamable HTTP ('closing the SSE response stream is the cancellation signal; the server MUST treat a client disconnect as cancellation') in the same release that REMOVED stream resumability (SEP-2575: 'a broken response stream loses the in-flight request; clients MUST re-issue it as a new request with a new request ID'). Those are ONE network event with TWO mandated readings - 'stop' to the server, 'run it again under a fresh identity' to the client - with nothing synchronising them and no bound on stop latency, which also means requestId-keyed server-side deduplication is PROVABLY unable to match the retry the spec itself requires. Given a captured JSON-RPC session (JSONL, both transports) plus an effect profile declaring when each tool's side effects become DURABLE, undertow computes the RESIDUE WINDOW [cancel_ts, last observed server activity] of every cancelled request and classifies each declared effect as COMMITTED (durable before the cancel arrived) / RACING (landed inside the window - the cancel lost) / AVOIDED, flagging any external uncompensated effect in the first two as UNRECOVERABLE; and fingerprints requests (canonical JSON minus _meta and declared volatile args) to pair a cancelled request with its re-issue and decide whether a STABLE IDEMPOTENCY KEY survived the transition (suppressed) or the work provably ran twice under two different request IDs (confirmed double dispatch). 26 rules in 6 families - cancel-channel conformance (late response, unreleased slot per codex #20925, cancel of unknown/completed requests, the new 2026-07-28 MUST NOT on server-sent notifications/cancelled outside subscriptions/listen, disconnect not honoured, post-cancel emission, orphan/late progress tokens), liveness (progress-keepalive timeout defeat via the spec's own MAY-reset-the-clock clause, missing timeout ceiling, excessive stop latency), the duplication channel, task lifecycle (a task handle outliving the cancellation that should have stopped it; cooperative tasks/cancel never confirmed terminal), effect residue and capture coverage - with severity-weighted scoring, per-rule deduction caps, categorical grade clamps and a CI exit-code gate. Ships four ground-truth fixtures whose exact rule sets are asserted by tests: conformant-stdio (zero findings, 100/A), conformant-http (one INFO - duplication suppressed by a stable key, 100/A), defective-stdio (10 rules, 47/F) and defective-http (23 rules, 2 critical / 9 high / 14 medium, 0/F) built around a money-moving flagship where a stop is ignored for 1200ms, the debit commits INSIDE the residue window uncompensated, and the client re-issues the identical transfer 800ms later under a new request ID. 5137 LOC, 211 tests, zero runtime dependencies | [dev-experiment-042](https://github.com/freyesperales/dev-experiment-042) | per-repo |
| 043 | 2026-09-02 | skein - offline, LLM-free, dependency-free (pure-stdlib) SERIALIZABILITY & INTERFERENCE AUDITOR for the AGENT PARALLEL TOOL-CALL plane: the series' first entry on the INTERLEAVING leg. Experiments 001-042 all audit calls ONE AT A TIME - what a call may do (001-010), what one channel promises (011-021), what the seams between channels leak (022), what happens on retry (038), on cancel (042), on the way back (041) - and every one of them assumes the calls happened IN SOME ORDER. skein audits the case where they did not. The thesis is a collision between two 2026 releases: MCP 2026-07-28 deleted protocol sessions and told servers that need cross-call state to mint an explicit handle and have the model PASS IT BACK AS AN ORDINARY TOOL ARGUMENT (SEP-2567), while both major labs shipped wide parallel-subagent modes and orchestration harnesses that batch every tool call a model emits in one turn. Together those rebuild the oldest bug in databases out of parts nobody thinks of as a database: shared mutable state addressed by a value the MODEL holds, mutated by calls dispatched with no ordering between them, against effects that sometimes cannot be undone. The 2026 literature saw it and answered with RUNTIMES - Atomix's transactional settlement (arXiv 2602.14849), CoAgent's monotonic pre-order (arXiv 2606.15376), three verified Rust runtimes realising an L0-L4 consistency hierarchy (arXiv 2606.17182) - each correct, each asking you to replace your execution layer, and none able to say anything about the sessions you already logged. skein is the cheap half nobody shipped. Given a captured session (JSONL, with actor identity so a main agent and its subagents are ONE capture, and batch identity so the model's issue order is recoverable) plus a declarative effect profile saying what each tool reads / writes / appends / deletes / enumerates, it builds the CONFLICT GRAPH of the observed schedule - a provably-ordered conflict contributes one directed edge, a CONCURRENT conflict contributes both, because the capture does not say which way it went - and a strongly connected component is then a set of invocations for which NO SERIAL ORDER reproduces what happened. 26 rules in 6 families: serialization (non-serializable region via Tarjan SCC with a shortest-cycle witness, lost update, dirty read, write skew across a declared invariant, non-repeatable read, tool-effect reordering), staleness (a raced read whose value reaches a LATER call's arguments - the race that became a wrong ACTION, not just a wrong value; phantom enumeration; read/write and read/delete races), causality (provenance violation: a call issued carrying a value whose first appearance anywhere in the capture is the result of a call that had not finished - the model minted a handle the server was supposed to mint), irreversibility (uncompensable external effect INSIDE a conflict cycle, which is critical because the schedule has no serial equivalent AND cannot be rolled back to obtain one), isolation hygiene (exclusive violated, a parallel_safe claim contradicted by an observed conflict, cross-actor write collision - the worktree isolates the working DIRECTORY, not the ledger) and coverage, where every undecidable question becomes a finding rather than a silent skip. Also emits a SERIALIZATION PLAN: each observed batch re-partitioned into conflict-free waves by greedy colouring, so the output is a fix and not only a verdict. STA01/STA02/CAU01/CAU03/SER06 are the detection-side counterparts of the four anomalies formalised in TLA+ by arXiv:2606.17182 (stale-generation, phantom-tool, causal-cascade, tool-effect reordering). Severity-weighted scoring with per-rule deduction caps and categorical grade clamps, text + JSON, CI exit-code gate. Four ground-truth fixtures whose exact rule sets, counts, scores and grades are asserted by tests: clean-coding and clean-commerce both 0 findings / 100 / A (they exercise append/append commutativity, prefix disjointness, an exclusive tool scheduled correctly and a benign cross-actor overlap - every path a sloppy conflict matrix false-positives on), defective-coding (14 rules, 10 high / 11 medium / 1 low, 0/F: two fs.edit calls on one file in one batch, a subagent writing it from outside, a listing racing all three, a config read whose stale value reaches a later git.commit) and defective-commerce (17 rules, 2 CRITICAL / 11 high / 16 medium / 2 low, 0/F: two payments.charge and a cart.add_item in one batch form a 3-invocation region with an uncompensable ledger write inside it). 7597 LOC, 563 tests, zero runtime dependencies | [dev-experiment-043](https://github.com/freyesperales/dev-experiment-043) | per-repo |
